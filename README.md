# Installing Cashier V10 with Laravel v6

Run the following commands:

 - composer install
 - npm install
 - php artisan migrate
 
Edit `.env` and set your Stripe keys:

```
STRIPE_KEY=
STRIPE_SECRET=
```
